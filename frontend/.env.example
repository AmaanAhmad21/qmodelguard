# Frontend configuration
# Copy to .env to override (optional; defaults work for local dev).

# Backend API URL for Vite dev proxy
# Frontend /api/* is proxied to this URL.
# VITE_API_PROXY_TARGET=http://localhost:8000
